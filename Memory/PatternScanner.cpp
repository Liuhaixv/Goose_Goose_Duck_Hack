#include "PatternScanner.h"

int64_t PatternScanner::FindPattern(IN std::vector<byte> Pattern)
{
    //TODO+6
    //TODO:
    /*
    Pattern.shrink_to_fit();

    for (int64_t i = 0; i < *Size; i++)
    {
        auto DoesMatch{ true };

        for (int64_t j = 0; j < Pattern.size(); j++)
        {
            if (Pattern[j] == 0) continue;
            if (Pattern[j] != Data[i + j]) { DoesMatch = false; break; }
        }

        if (DoesMatch) return i;
    }
    */
    return 0;
}
